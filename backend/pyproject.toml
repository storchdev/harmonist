[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "basic-pitch[tf]>=0.4.0",
    "flask>=3.1.2",
    "flask-cors>=6.0.2",
    "numpy>=1.26.4",
]
